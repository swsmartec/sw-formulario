export interface BaseFormProps {
  keyStorage: string;
  valuesForm: object;
}

export interface BaseStep {
  title: string;
  icon: string;
}

export interface BaseFormRequest {
  Identificacion: string;
  RazonSocial: string;
  DetalleFormulario: string;
}
